# Directory containing the deployed artifacts (labels.json, {feat}_svm.pkl,
# viz/{feat}/*). Defaults to models/instrument_classifier relative to the repo
# root if unset.
MODEL_DIR=./models/instrument_classifier
